future works: combine 2d and 3d proposals. end-to-end optimization.

\todo{CNN on images $\rigthtarrow$ PointNet on 3D point cloud. Joint force from RGB image and depth data. simple and clear pipeline with great flexibility (generalize to both indoor and outdoor). good result and nice runtime efficiency. }